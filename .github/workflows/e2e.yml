name: E2E pipeline

on:
  workflow_dispatch:
    inputs:
      job:
        description: E2E job to run
        required: true
        default: pipeline-calibration
        type: choice
        options:
          - pipeline-calibration
          - e2e-s2-l1b

jobs:
  pipeline-calibration:
    name: Calibration campaign smoke
    if: inputs.job == 'pipeline-calibration'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    continue-on-error: true
    env:
      OUTPUT_DIR: cal-store
      S2_L1B_INPUT: cal-store/inputs/placeholder
      S2_L0_INPUT: cal-store/inputs/placeholder
      S2_GIPP_DIR: cal-store/inputs/placeholder
      S2_AUX_DIR: cal-store/inputs/placeholder
      S2_NDET: "64"
      S2_CAL_LINES: "64"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install (numpy + zarr only)
        run: |
          python -m pip install --upgrade pip
          pip install numpy zarr
          pip install -e . --no-deps
      - name: Run calibration pipeline
        run: python scripts/run_pipeline.py calibration
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: calibration-report
          path: |
            cal-store/report/
            cal-store/quicklook/

  e2e-s2-l1b:
    name: Windowed nominal-chain E2E
    if: inputs.job == 'e2e-s2-l1b'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    continue-on-error: true
    env:
      OUTPUT_DIR: e2e-s2-l1b-store
      S2_L1B_INPUT: e2e-s2-l1b-store/inputs/placeholder
      S2_L0_INPUT: e2e-s2-l1b-store/inputs/placeholder
      S2_GIPP_DIR: e2e-s2-l1b-store/inputs/placeholder
      S2_AUX_DIR: e2e-s2-l1b-store/inputs/placeholder
      S2_PHASES: fetch-l1a,preflight,package,ground-decode,l0-decode,validate,quicklook,report
      S2_LINES: "4096"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install eopf + msi-processor + generator
        run: |
          python -m pip install --upgrade pip
          git clone --depth 1 https://github.com/AstroCan17/msi-processor.git /tmp/msi-processor
          pip install "eopf==2.8.1" /tmp/msi-processor
          pip install -e . pytest
      - name: Run E2E pipeline
        run: python scripts/run_pipeline.py
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-s2-l1b-report
          path: |
            e2e-s2-l1b-store/report/
            e2e-s2-l1b-store/quicklook/
