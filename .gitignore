# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local sample-data archive only; the data/ E2E store (input + output products, quicklooks)
# IS tracked and synced through git. Mind the push size before dropping large real inputs
# (e.g. the ~500 MB bucket L1A) into data/input/.
data/s2_dataset/

# Build / docs
build/
dist/
docs/_build/

# Scratch / OS
.DS_Store
*.tmp
scratchpad/

# Sphinx docs build artifacts
/public/
docs/_build/
docs/api/
